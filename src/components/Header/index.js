import React from "react";

// use below Header component to render the table

function Header(){
    return (
    <span className="bg-primary">
    <h1 className="p-5 text-center bg-primary">Simple Employee Directory</h1>
    </span>
    )
}

export default Header;